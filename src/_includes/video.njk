{% macro media(src, path) %}

{# <figure style='--ratio:{{ ratio }};' class='video media {{effects}}'> #}
{% set path="http://localhost:8080/content/img/" %}  
{% set autoplay = true  %}

<figure style='' class='video media'>
    <video preload="none" {% if autoplay %} autoplay {% endif %} muted playsinline loop >
      {# SET THIS LATER, ADD AN IMAGE poster="{{ path }}video/{{ src }}.jpg" #}

      {# <source src="{{ path }}video/{{ src }}.webm" type="video/webm"> #}
      <source src="{{ path }}{{ src }}.mp4" type="video/mp4">
    </video>
    {# {% if caption %}
      <figcaption><a href="{{ link }}" target='_blank'>{{ caption }}</a></figcaption>
    {% endif%} #}
  </figure>

{% endmacro %}